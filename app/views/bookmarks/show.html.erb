<p>
  <%= link_to 'Edit Bookmark', edit_bookmark_path(@bookmark) %> |
  <%= link_to 'Delete Bookmark', bookmark_path(@bookmark), method: :delete, confirm: 'Are you sure you want to delete this bookmark?' %>
</p>
<h2><%= @bookmark.title %></h2>
<p><%= @bookmark.url %></p>
