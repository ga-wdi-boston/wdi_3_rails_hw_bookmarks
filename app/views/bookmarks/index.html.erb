<h3>Look at all these bookmarks!</h3>

<table>
  <tr>
    <td>Title</td>
    <td>Category</td>
    <td>Favorite?</td>
  </tr>
  <% @bookmarks.each do |bookmark| %>
    <tr>
      <td><%= bookmark.title %></td>
      <td><%= bookmark.category %></td>
      <td><%= bookmark.is_favorite %></td>
    </tr>
  <% end %>
</table>
