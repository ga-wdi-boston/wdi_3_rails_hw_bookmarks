<p><%= link_to 'New bookmark', new_bookmark_path %></p>

<h2>Here's a list of your Bookmarks:</h2>
<ol>
  <% @bookmarks.each do |bookmark| %>
  	<% if bookmark.favorite == true %>

  	<strong><li><%= link_to truncate(bookmark.title, length: 100), bookmark %>   <em>:</em> <%= link_to bookmark.URL, bookmark.URL %></li>
  	<em>Comment: <%= bookmark.comment %></em></strong>
  	<% else %>
  	<li><%= link_to truncate(bookmark.title, length: 100), bookmark %>   <em>:</em> <%= link_to bookmark.URL, bookmark.URL %></li>
  	<em>Comment: <%= bookmark.comment %></em>
  	<% end %>
  <% end %>
</ol>
