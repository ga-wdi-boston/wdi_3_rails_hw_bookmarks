<%= form_for @comment do |f| %>
  <%= f.hidden_field :bookmark_id %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
