<div class="container">
  <div class="col-md-4">
    <h2>Edit your comment:</h2>
    <%= form_for([@bookmark, @comment]) do |f| %>
      <%= f.text_field :body, class: 'form-control' %><br>
      <%= f.submit :submit, value: 'Update', class: 'btn btn-default' %>
    <% end %>
  </div>
</div>

